name: descriptive-workflow-name

on:
  push:
    branches: [ master ] 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master  
    - name: build Lab4
      run: cd Lab4 && make 
    - name: make clean
      run: cd Lab4 && make clean
